ESSENTIAL:

- Route back to form if no data
- Rate My Teacher links clickable
- Check for time errors on Course Form
- Loader when generating schedules
- Show not found page for 0 schedules
- Button to go back
- Preserve form state with url params
- Change cell colors based on teacher rating
- Add text to all tooltips

CODE STRUCTURE:

- Convert Start, End, Duration time on the backend
- Put each component in its own folder
- avoid antipatterns
- Remove useless server routes
- Handle errors better
